<html>
    <head>
        <meta charset="utf-8">
        <title>
            홈
        </title>
        <style>
             * {
                margin: 0px;
                box-sizing: border-box;
                font-size: 11px;
            }

            html {
                width: 100%;
                display: flex;
                justify-content: center;
                padding-top: 20px;
                padding-bottom: 20px;
            }

            body {
                width: 45%;
                border: 1px solid black;
            }

            input{
                border: 1px solid black;
                border-radius: 3px;
                line-height: 35px;
                font-size: 12px;
                padding-left: 10px;
                padding-right: 10px;
            }

            .wrapper {
                padding: 10px;
            }

            .title {
                text-align: center;
                font-weight: 700;
            }
        </style>
    </head>
    <body>
        <div class="wrapper">
            <div class="title"><h1 style="font-size: 21px;">메인페이지</h1></div>
            <% if(!isLogin){ %>
                <a href="/login">로그인</a>
            <% } else { %>
                <a href="/userInfo">정보보기</a>
                <a href="/logout">로그아웃</a>
            <% } %>    
        </div>
    </body>
</html>